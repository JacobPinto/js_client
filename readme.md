### A test to build a JS client.

# Compile details
> `npm run build`  | builds TS and Vite (bundler)
> `npm run dev`    | serve page at `/test`

# Cleanup:
> `npm run clean`  | clean generated JS files and Vite 


Some layout details:

![typical layout](share/layout.png)
